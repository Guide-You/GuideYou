<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guideyou.repository.interfaces.admin.BoardRepository">

	<insert id="insert" parameterType="com.guideyou.dto.admin.BoardDto">
		insert into board_contents (type, writer, title,
		content) values (#{type}, #{writer},
		#{title}, #{content})
	</insert>
	
	<select id="findAll" resultType="com.guideyou.repository.entity.Board">
		select * from board_contents 
	</select>
	<!-- 페이징 처리 쿼리 -->
	<select id="findAllPgs" resultType="com.guideyou.repository.entity.Board">
		select * from board_contents 
		limit = #{limit}, offset = #{offset}
	</select>
	<!-- 전체 데이터 개수 쿼리 -->
	<select id="getAllPgCount" resultType="int">
	 	select count(*) from board_contents
	</select>
	<!-- 삭제 쿼리 -->
	<delete id="deleteById">
		delete from board_contents where id = #{id}	
	</delete>
	<!-- update 쿼리 -->
	<!-- 수정 쿼리 -->
</mapper>